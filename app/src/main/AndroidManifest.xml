<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oscarapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VotoFinalActivity"></activity>
        <activity android:name=".DetalhesFilme" />
        <activity android:name=".ListCell" />
        <activity android:name=".ListFilmes" />
        <activity android:name=".VotarDiretorActivity" />
        <activity android:name=".VotarFilmeActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".Cadastrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>